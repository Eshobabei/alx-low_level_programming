#include "main.h"

/**
 * print_alphabet_x10(void) - prints 10x the alphabets in lower case
 * Return: void
 */

void print_alphabet_x10(void)
{
	char c;
	int i = 0;

	while (i <= 0)
	{
		for (c = 'a'; c <= 'z'; c++)
	{
		_putchar(c);
	}
		_putchar('\n');
		i++;
	}
}
